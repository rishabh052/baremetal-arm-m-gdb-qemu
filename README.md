# baremetal-arm-m-gdb-qemu
This repo contains bare metal code to be run arm-m controller emulated on qemu. The emulated board is STM32 Discovery, and various files related to startup, linker, memory mapped registers, and mbox functionality are included.
